<div>
<!--   <b>ID: <%= subscription.id %></b> Subscripton type: <%= subscription.subscribable_type.to_s.humanize %>, User Email: <%= subscription.user.email.to_s %> -->

	<tr style="border-bottom:1px #ccc dashed;">
		<td>
			<%= subscription.subscribable.project.name %>
		</td>
		<td>
<%= subscription.subscribable.title %>
</td>

<% if (can? :manage, :all) %>    
      <td>
      	<%= subscription.user.email.to_s %>
      </td>
<% end %>

<td>
	<% @unsub = 'subscriptions/' + subscription.id.to_s + '/unsub' %>
	  <span><%= link_to "Unsubscribe", @unsub %> </span>
</td>
		</tr>

</div>